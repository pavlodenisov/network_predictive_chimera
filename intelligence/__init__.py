"""Chimera Network Intelligence — internal analytical intelligence platform.

A high-integrity analytical system: every recommendation explainable, every score
decomposable, every fact provenanced, every inference labelled, every model versioned,
every historical output reproducible, every unknown left unknown.

See ``docs/`` for the specification of record and ``CLAUDE.md`` for engineering rules.
"""

__version__ = "0.1.0"
