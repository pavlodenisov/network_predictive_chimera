# Chimera Network Intelligence — backend runtime + dev dependencies
# Python 3.11+ (developed/verified on 3.14). SQLite needs no driver; Postgres is optional.

fastapi>=0.115
uvicorn>=0.30
sqlalchemy>=2.0.30
pydantic>=2.7
pydantic-settings>=2.2
alembic>=1.13
PyYAML>=6.0
python-dateutil>=2.9
httpx>=0.27
feedparser>=6.0
structlog>=24.1

# --- dev / test / quality ---
pytest>=8.2
pytest-cov>=5.0
ruff>=0.5
mypy>=1.10
types-python-dateutil
types-PyYAML

# --- optional: Postgres driver (uncomment for production DATABASE_URL=postgresql+psycopg://...) ---
# psycopg[binary]>=3.2
